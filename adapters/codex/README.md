# Codex adapter

The Codex adapter remains distributed through `plugins/careful/` for the initial multi-harness compatibility release. Its skills reference the portable policy at `core/policy.md`; install it from the repository marketplace so that the portable core remains available. Its host-specific manifest and marketplace metadata remain in the existing Codex package.

Use the installation and migration instructions in [docs/adoption.md](../../docs/adoption.md).
